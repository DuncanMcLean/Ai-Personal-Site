# Server Configuration
PORT=3000

# Email Notifications Setup (Choose one option)

# Option 1: SendGrid (Recommended - No personal email credentials needed)
# 1. Sign up at https://sendgrid.com (free tier: 100 emails/day)
# 2. Create API key in SendGrid dashboard
# 3. Add your domain or use a verified sender
SENDGRID_API_KEY=your-sendgrid-api-key-here

# Option 2: Gmail (Alternative)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# For Gmail, you'll need to:
# 1. Enable 2-factor authentication
# 2. Generate an App Password
# 3. Use the App Password instead of your regular password

# Database Configuration (if you want to store submissions)
# DATABASE_URL=your-database-connection-string 